//hide modal
//    $('#new_employer_modal').modal('hide');
//add row
//    $('#multiselect_table').append("<%# j render partial: 'table_row', locals: {employer: @employer } %>");

//refresh the page
//window.location.reload();
//oTable.ajax.reload();
//oTable.draw(true);
oTable.row.add([
        '<input type="checkbox" name="delete[<%= @employer.id %>]" id="delete_<%= @employer.id %>" value="1">',
        '<a href="<%= edit_employer_path(@employer) %>" ><%= @employer.first_name %> </a>',
        '<%= @employer.position %>',
        '<%= @employer.office %>',
        '<%= @employer.age %>',
        '<%= @employer.start_date %>',
        '<%= @employer.salary %>'
]).draw();

//$('#multiselect_table INPUT').click(function() {
//    $(this).parent().parent().toggleClass('success');
//});

$('#delete_<%= @employer.id %>').click(function() {
    $(this).parent().parent().toggleClass('success');
});

//$('#multiselect_table tbody').on( 'click', 'tr', function () {
//    $(this).toggleClass('selected');
//} );

//clear modal error
$('#new_employer_modal').modal_success();